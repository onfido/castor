// generated by Theo (https://github.com/salesforce-ux/theo), edit `tokens.json` instead
@mixin tokens() {
{{#each props as |prop|}}
  --ods-{{prop.category}}-{{kebabcase prop.name}}: {{prop.value}};
{{/each}}
}
